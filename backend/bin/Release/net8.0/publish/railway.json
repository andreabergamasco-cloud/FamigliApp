{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish -c Release -r linux-x64 --self-contained true -o out"
  },
  "deploy": {
    "startCommand": "./out/backend --urls http://0.0.0.0:8080"
  }
}