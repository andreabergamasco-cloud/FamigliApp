{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "dotnet restore && dotnet publish -c Release -o out"
  },
  "deploy": {
    "startCommand": "dotnet out/backend.dll --urls http://0.0.0.0:8080"
  }
}